# GDP-Zero 论文复现指南（中文版）

## 快速开始

### 第一步：环境配置

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 接口设置
打开core/gen_models  :
API_TOKEN = os.environ.get("OPENAI_API_KEY", "sk-ejyFNvi45V3BzCgKOEHhN8mc0Qt8lLS9xpZPSLZVeCgCADKl")
BASE_URL = os.environ.get("OPENAI_BASE_URL", "https:/yinli.one/v1")

# 3. 设置 PYTHONPATH（必须！）
cd /root/autodl-tmp/MCT/GDPZero
export PYTHONPATH=$(pwd)
```

### 第二步：运行实验

**方式一：使用快速启动脚本（推荐）**
```bash
./quick_start.sh
```

**方式二：手动运行**

#### 主实验 - GDP-Zero

##### p4g数据集
```bash
# 配置1: n=10, k=3, Q_0=0.25（快速测试）
python runners/gdpzero.py \
    --output outputs/gdpzero_10sims_3rlz_0.25Q0_20dialogs.pkl \
    --llm gpt-3.5-turbo \
    --num_mcts_sims 10 \
    --max_realizations 3 \
    --Q_0 0.25 \
    --num_dialogs 20

# 配置2: n=20, k=3, Q_0=0.0
python runners/gdpzero.py \
    --output outputs/gdpzero_20sims_3rlz_0.0Q0_20dialogs.pkl \
    --llm gpt-3.5-turbo \
    --num_mcts_sims 20 \
    --max_realizations 3 \
    --Q_0 0.0 \
    --num_dialogs 20

# 配置3: n=50, k=3, Q_0=0.0（完整实验）
python runners/gdpzero.py \
    --output outputs/gdpzero_50sims_3rlz_0.0Q0_20dialogs.pkl \
    --llm gpt-3.5-turbo \
    --num_mcts_sims 50 \
    --max_realizations 3 \
    --Q_0 0.0 \
    --num_dialogs 20
```
##### cb数据集
```bash
# 配置1: n=10, k=3, Q_0=0.25（快速测试）
python runners/gdpzero.py \
    --dataset cb \
    --output outputs/cb/gdpzero_10sims_3rlz_0.25Q0_20dialogs.pkl \
    --llm gpt-3.5-turbo \
    --num_mcts_sims 10 \
    --max_realizations 3 \
    --Q_0 0.25 \
    --num_dialogs 20

# 配置2: n=20, k=3, Q_0=0.0
python runners/gdpzero.py \
    --dataset cb \
    --output outputs/cb/gdpzero_20sims_3rlz_0.0Q0_20dialogs.pkl \
    --llm gpt-3.5-turbo \
    --num_mcts_sims 20 \
    --max_realizations 3 \
    --Q_0 0.0 \
    --num_dialogs 20

# 配置3: n=50, k=3, Q_0=0.0（完整实验）
python runners/gdpzero.py \
    --dataset cb \
    --output outputs/cb/gdpzero_50sims_3rlz_0.0Q0_20dialogs.pkl \
    --llm gpt-3.5-turbo \
    --num_mcts_sims 50 \
    --max_realizations 3 \
    --Q_0 0.0 \
    --num_dialogs 20
```



#### Baseline - Raw Prompting

##### p4g数据集
```bash
python runners/raw_prompting.py \
    --output outputs/chatgpt_raw_prompt.pkl \
    --llm gpt-3.5-turbo
```

##### cb数据集
```bash
python runners/raw_prompting.py \
    --dataset cb \
    --output outputs/cb/chatgpt_raw_prompt.pkl \
    --llm gpt-3.5-turbo
```


### 第三步：评估结果

#### p4g数据集

##### 与人类回复对比
```bash
python test.py \
    -f outputs/chatgpt_raw_prompt.pkl \
    --output outputs/eval_vs_human.pkl \
    --judge gpt-3.5-turbo
```

##### Head-to-Head 对比
```bash
python test.py \
    -f outputs/gdpzero_10sims_3rlz_0.25Q0_20dialogs.pkl \
    --h2h outputs/chatgpt_raw_prompt.pkl \
    --output outputs/eval_h2h.pkl \
    --judge gpt-3.5-turbo
```

#### cb数据集

##### 与人类回复对比
```bash
python test.py \
    --dataset cb \
    -f outputs/chatgpt_raw_prompt.pkl \
    --output outputs/cb/eval_vs_human.pkl \
    --judge gpt-3.5-turbo
```

##### Head-to-Head 对比
```bash
python test.py \
    --dataset cb \
    -f outputs/cb/gdpzero_10sims_3rlz_0.25Q0_20dialogs.pkl \
    --h2h outputs/cb/chatgpt_raw_prompt.pkl \
    --output outputs/cb/eval_h2h.pkl \
    --judge gpt-3.5-turbo
```

## 参数说明

- `--llm`: 使用的模型（gpt-3.5-turbo / code-davinci-002 / chatgpt）
- `--num_mcts_sims`: MCTS 模拟次数（n），越多越准确但越慢
- `--max_realizations`: 每个状态的文本实现数量（k），默认3
- `--Q_0`: 未访问动作的初始Q值，影响探索（0.0-0.25）
- `--num_dialogs`: 测试的对话数量（默认20）

## 预期结果

- **GDP-Zero vs Human**: Win rate ~93-95%
- **GDP-Zero vs Raw Prompting**: Win rate ~55-60%

## 常见问题

1. **ModuleNotFoundError**: 检查 PYTHONPATH 是否设置
2. **API Key 错误**: 检查环境变量是否正确设置
3. **数据文件不存在**: 确保 `data/p4g/300_dialog_turn_based.pkl` 存在

## 时间估算

- 单次实验（20对话，10次模拟）: 1-2小时
- 完整复现: 5-10小时

## 详细文档

查看 `REPRODUCTION_GUIDE.md` 获取完整说明。
先试一下配置1和baseline，比较一下效果，能不能到50.9%左右；然后看看配置3，能不能到57.8%。
看看GDP-ZERO和人类比能不能到90%。
